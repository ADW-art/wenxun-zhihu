# Codex 通用网页与智能体项目启动构建指南

> 用途：将本文件放在项目仓库根目录，交给 Codex 读取并执行。它适用于从零启动或规范化一个网页项目，并在项目属于智能体、RAG、多模型或工具调用系统时追加专项基础设施。

---

## 1. Codex 的任务

你的职责不是立刻生成代码，而是依次完成：

1. 检查现有项目和环境；
2. 与用户澄清目标、范围和约束；
3. 输出可比较、可确认的方案；
4. 在用户确认方案后建立项目文档和工程基础设施；
5. 验证项目可以安装、启动、测试和继续开发；
6. 留下可以跨会话、跨开发者恢复的项目状态。

必须采用“先对齐，后构建”的方式。任何关键决策未确认前，不得直接选择技术栈、架构、数据库、认证方案、模型供应商或部署平台。

---

## 2. 强制原则

### 2.1 禁止主观臆断

不得自行假设以下信息：

- 项目目标、目标用户和核心使用场景；
- 必须实现的功能和明确不做的功能；
- Web、移动端、桌面端或响应式适配范围；
- 前后端技术栈、数据库、缓存和消息队列；
- 登录、权限、支付、文件上传或第三方集成；
- 部署平台、域名、预算、并发量和可用性目标；
- 数据来源、许可证、隐私和合规要求；
- 智能体模型、API、记忆、RAG、工具调用和多智能体方案；
- UI 风格、品牌色、组件库和交互方式；
- 测试范围、验收阈值和比赛或业务评分标准。

信息不足时必须：

1. 明确指出缺失信息；
2. 说明该信息会影响什么决策；
3. 提供 2～3 个可比较选项；
4. 给出推荐项及理由，但标记为“建议”，不能视为用户决定；
5. 等待用户确认。

只有可随时撤销且不影响产品方向的小型实现细节，才允许采用行业常见默认值。采用前仍需在方案中列明。

### 2.2 聊天不是项目真相源

项目中的已确认文档、规格、测试和代码优先于聊天记录。推荐优先级：

```text
用户最新明确决定
  > 项目目标与需求文档
  > 已确认的规格和 ADR
  > 架构、API、数据与 UI 文档
  > 测试和验收标准
  > 当前代码
  > 历史聊天记录
```

发现冲突时停止实现，列出冲突并请求用户决定。确认后先更新真相源，再继续修改代码。

### 2.3 阶段门控

未经用户确认，不得跨越以下门槛：

- 未确认需求，不进入架构设计；
- 未确认架构，不创建完整工程；
- 未确认数据和外部接口，不实现相关模块；
- 未定义验收方式，不宣称功能完成；
- 未通过验证，不宣称基础设施构建成功。

### 2.4 最小充分原则

- 不为了“看起来完整”引入不需要的框架和服务；
- 不预先建设尚无明确需求的微服务、消息队列、复杂权限或多智能体系统；
- 优先建立可运行、可测试、可扩展的最小闭环；
- 所有新增复杂度都必须对应明确需求、风险或验收指标。

---

## 3. 总体执行流程

```text
阶段 0：只读检查
  ↓
阶段 1：用户访谈与需求确认
  ↓
阶段 2：方案设计与用户审批
  ↓
阶段 3：项目文档落地
  ↓
阶段 4：工程基础设施搭建
  ↓
阶段 5：最小可运行闭环
  ↓
阶段 6：测试、审查与交付
  ↓
阶段 7：记录状态并进入后续迭代
```

---

## 4. 阶段 0：只读检查

开始时只允许读取和分析，不得修改文件。

检查内容：

1. 当前目录是否为 Git 仓库；
2. 是否已有代码、文档或配置；
3. 是否存在未提交修改；
4. 是否存在 `AGENTS.md`、`README.md`、规格或任务文件；
5. 当前语言、包管理器、框架和版本；
6. 可用的测试、Lint、格式化、构建和启动命令；
7. 环境变量、外部服务和密钥需求；
8. 是否存在 CI、容器、部署或数据库迁移配置；
9. 是否存在用户已有修改，禁止覆盖或删除；
10. 当前项目是否已使用 OpenSpec 或其他规格驱动工具。

输出《项目现状报告》：

```md
## 项目现状

- 项目类型：
- 当前技术栈：
- 已有功能：
- 已有文档：
- 已有测试：
- 未提交修改：
- 可复用内容：
- 主要缺口：
- 潜在风险：
- 下一步需要用户确认的问题：
```

如果是空目录，明确说明“这是新项目”，然后进入需求访谈。

---

## 5. 阶段 1：用户访谈与需求确认

不要一次抛出几十个问题。按照决策依赖分批询问，每批优先询问最影响后续设计的问题。

### 5.1 第一批：项目定义

至少确认：

- 项目解决什么问题；
- 主要用户是谁；
- 用户完成任务的核心路径；
- 项目的核心价值；
- 当前是原型、比赛项目、课程项目、内部工具还是生产产品；
- 必须完成的功能；
- 明确不做的功能；
- 计划周期、团队人数和现有能力；
- 最终交付物和成功标准。

### 5.2 第二批：网页产品范围

按需确认：

- 页面和主要用户流程；
- 是否需要响应式、移动端适配或无障碍；
- 是否需要账户、角色和权限；
- 是否需要后台管理端；
- 是否需要搜索、上传、导出、通知或实时通信；
- 数据是用户输入、内部数据还是第三方数据；
- 是否有现成 UI、原型、设计稿或品牌规范；
- 是否有 SEO、国际化、分析统计需求。

### 5.3 第三批：技术与运行约束

按需确认：

- 用户是否已经指定技术栈；
- 目标运行环境和部署平台；
- 是否必须前后端分离；
- 数据库类型、数据规模和一致性要求；
- 第三方 API 和账号是否已经具备；
- 安全、隐私、合规与许可证要求；
- 预算、并发、延迟和可用性目标；
- 是否需要 Docker、CI/CD、监控和备份；
- 是否需要支持离线或接口故障降级。

### 5.4 决策记录

将结论整理成两类：

```md
## 已确认

- 决策：
  - 用户结论：
  - 影响范围：

## 待确认

- 问题：
  - 为什么必须确认：
  - 选项：
  - 建议：
```

所有影响范围、架构、数据、安全、成本或外部接口的待确认项解决后，才能进入下一阶段。

---

## 6. 阶段 2：方案设计与用户审批

根据用户已确认信息生成《项目方案》，至少包括以下内容。

### 6.1 产品方案

- 项目目标与非目标；
- 目标用户；
- 核心场景；
- 页面清单；
- 用户流程；
- MVP 范围；
- 后续扩展范围；
- 验收标准。

### 6.2 技术方案

- 前端、后端和数据库候选；
- 模块划分；
- 数据流；
- API 边界；
- 身份认证和权限方案；
- 部署与环境方案；
- 测试、日志和监控方案；
- 安全和故障降级方案。

### 6.3 方案比较规则

重大技术选择尚未由用户指定时，提供对比表：

| 方案 | 优点 | 缺点 | 开发成本 | 维护成本 | 风险 | 适用条件 |
|---|---|---|---:|---:|---|---|
| 方案 A | | | | | | |
| 方案 B | | | | | | |

推荐方案必须说明：

- 为什么适合当前目标；
- 为何不选其他方案；
- 哪些事实来自用户；
- 哪些是工程建议；
- 未来什么条件下需要调整。

### 6.4 审批门槛

询问用户是否确认：

1. 项目目标和 MVP；
2. 页面与功能范围；
3. 技术栈；
4. 数据和外部接口；
5. 部署方式；
6. 验收标准；
7. 预计创建的基础设施和文件。

只有得到明确确认，才能开始写入项目。

---

## 7. 阶段 3：通用 Markdown 文档

根据项目规模创建必要文档。禁止创建无人维护的空文档。

### 7.1 核心文档

| 文件 | 作用 | 必需性 |
|---|---|---|
| `README.md` | 项目介绍、安装、启动、测试、构建和常见问题 | 必需 |
| `AGENTS.md` | Codex 的项目规则、命令、约束、禁止事项和完成定义 | 必需 |
| `docs/PROJECT_BRIEF.md` | 目标、用户、价值、范围、非目标和成功标准 | 必需 |
| `docs/REQUIREMENTS.md` | 功能需求、非功能需求、用户故事和验收条件 | 必需 |
| `docs/ARCHITECTURE.md` | 系统边界、模块、数据流、依赖和部署拓扑 | 必需 |
| `docs/TEST_PLAN.md` | 测试层级、覆盖范围、命令、测试数据和通过条件 | 必需 |
| `docs/PROJECT_STATE.md` | 当前里程碑、活动任务、阻塞项、已知问题和下一步 | 长期项目必需 |
| `docs/DEPLOYMENT.md` | 环境、配置、发布、回滚和故障排查 | 需要部署时必需 |

### 7.2 网页项目文档

| 文件 | 作用 | 创建条件 |
|---|---|---|
| `docs/UX_FLOW.md` | 页面、导航、用户流程、空状态和异常状态 | 存在用户界面 |
| `docs/UI_SPEC.md` | 视觉规范、组件、响应式和无障碍要求 | UI 需要统一规范 |
| `docs/API_SPEC.md` | 接口路径、请求、响应、错误码、认证和版本 | 存在前后端或外部接口 |
| `docs/DATA_MODEL.md` | 实体、字段、关系、约束、索引和迁移 | 存在持久化数据 |
| `docs/AUTHORIZATION.md` | 用户、角色、资源和权限矩阵 | 存在账户或权限 |
| `docs/SECURITY.md` | 威胁、敏感数据、输入校验、密钥和事件处理 | 涉及用户或外部数据 |
| `docs/OBSERVABILITY.md` | 日志、指标、追踪、告警和隐私处理 | 需要部署或长期运行 |

### 7.3 决策与协作文档

| 文件 | 作用 | 创建条件 |
|---|---|---|
| `docs/adr/NNN-title.md` | 记录重要技术决策、替代方案和后果 | 架构或依赖发生重要选择 |
| `docs/CODE_REVIEW.md` | PR 检查重点和禁止合并条件 | 两人以上协作 |
| `docs/CONTRIBUTING.md` | 分支、提交、PR 和贡献流程 | 团队或开源项目 |
| `docs/SESSION_START.md` | 新对话读取顺序和项目恢复步骤 | 经常更换 AI 会话 |
| `CHANGELOG.md` | 版本变化和迁移说明 | 有版本发布 |

### 7.4 文档一致性要求

- 同一事实只保留一个权威来源，其他文件使用链接引用；
- 文档必须写明“最后更新日期”和适用范围；
- 架构、接口或验收标准改变时必须同步更新文档；
- 文档不得记录真实密钥、Token 或私人数据；
- 不得用文档重复粘贴可以由代码、Schema 或脚本自动生成的信息；
- 过期内容必须更新或删除，不能继续误导后续会话。

---

## 8. `AGENTS.md` 最低内容

生成的 `AGENTS.md` 至少包括：

```md
# Project Instructions

## Read First
- docs/PROJECT_BRIEF.md
- docs/REQUIREMENTS.md
- docs/ARCHITECTURE.md
- docs/PROJECT_STATE.md

## Project Goal
- 项目目标：
- 当前阶段：
- 非目标：

## Repository Map
- 主要目录及职责：

## Commands
- 安装：
- 开发：
- Lint：
- 类型检查：
- 单元测试：
- 集成测试：
- 构建：
- 启动：

## Development Workflow
1. 修改前阅读相关规格和现有实现。
2. 复杂任务先提出计划，不直接编码。
3. 只修改完成任务所需的最小范围。
4. 修改后运行规定检查。
5. 更新相关文档和项目状态。

## Anti-Assumption Rules
- 不得自行改变产品范围。
- 不得猜测未确认的业务规则。
- 不得擅自更换技术栈和核心依赖。
- 发现规格、测试和代码冲突时停止并报告。

## Security
- 不提交真实密钥和用户数据。
- 不记录敏感请求和响应。
- 外部输入必须验证。

## Definition of Done
- 符合已确认需求和验收标准。
- 测试、Lint、类型检查和构建通过。
- 文档与实现一致。
- 无未说明的范围扩张。
- 已审查 Git diff 和已知风险。

## Code Review Rules
- 优先检查正确性、安全、数据损坏、兼容性和回归。
- 不把纯格式问题作为最高优先级问题。
```

---

## 9. 阶段 4：工程基础设施

### 9.1 版本控制

- 初始化 Git；
- 创建适合技术栈的 `.gitignore`；
- 保留用户已有修改；
- 默认分支保持可运行；
- 功能、修复和实验使用独立分支；
- 两个开发者或两个 AI 会话不得同时修改同一工作树；
- 并行开发优先使用 Git worktree。

推荐分支类型：

```text
main
feat/<name>
fix/<name>
experiment/<name>
chore/<name>
```

### 9.2 环境与依赖

- 固定运行时和包管理器版本；
- 提交依赖锁文件；
- 提供 `.env.example`，不得提交真实 `.env`；
- 区分开发、测试和生产环境；
- 所有必需环境变量必须有用途说明；
- 提供一条可靠的安装命令和一条开发启动命令。

### 9.3 代码质量

按技术栈配置：

- 格式化；
- Lint；
- 类型检查；
- 单元测试；
- 集成测试；
- 构建检查；
- 必要的端到端冒烟测试。

检查命令必须写入 `README.md` 和 `AGENTS.md`，并保证可以在干净环境运行。

### 9.4 CI

最低 CI 流程：

```text
安装依赖
  → 格式/Lint
  → 类型检查
  → 单元测试
  → 集成或冒烟测试
  → 构建
```

如果某项暂时无法自动化，必须记录原因、人工检查方法和后续任务。

### 9.5 安全基础

- 使用环境变量或密钥管理服务；
- 对输入、上传文件和外部响应进行验证；
- 设置合理的超时、重试和速率限制；
- 避免把敏感内容写入日志；
- 对破坏性操作增加确认和权限检查；
- 明确依赖升级和漏洞处理方法。

### 9.6 可观测性

根据项目规模建立：

- 结构化日志；
- 请求或任务关联 ID；
- 关键错误和性能指标；
- 外部 API 调用耗时与失败率；
- 健康检查；
- 部署版本或 Git commit 标识。

### 9.7 数据与迁移

如果存在数据库：

- 使用迁移工具，不直接手工修改生产结构；
- 提供初始化或种子数据；
- 明确备份与恢复方式；
- 测试数据与生产数据隔离；
- Schema 变更必须包含兼容和回滚考虑。

---

## 10. 是否使用 OpenSpec

满足任一情况时，建议使用 OpenSpec 或等价的规格驱动流程：

- 项目持续两周以上；
- 两人以上协作；
- 功能会多次反复调整；
- 经常更换 AI 会话或模型；
- 前端、后端、数据或智能体模块较多；
- 需求与实现容易发生偏移。

每个明显改变外部行为的功能使用独立变更目录：

```text
openspec/changes/<change-name>/
├── proposal.md   # 为什么修改、修改范围
├── specs/        # 行为和可验证需求
├── design.md     # 技术方案和权衡
└── tasks.md      # 实现步骤
```

工作顺序：

```text
explore → propose → 用户审阅 → apply → verify → archive
```

OpenSpec 不替代项目总览、架构、测试和部署文档。它负责“某次变更”，核心文档负责“整个项目长期状态”。

如果项目很小且生命周期短，可以使用单个 `docs/PLAN.md`，无需强行引入完整规格工具。

---

## 11. 阶段 5：最小可运行闭环

基础设施完成后，只实现一个能够验证整体架构的最小纵向功能：

```text
用户操作
  → 前端界面
  → 后端接口
  → 核心业务逻辑
  → 数据或外部服务
  → 返回结果
  → 日志和测试验证
```

最小闭环要求：

- 能从干净环境安装；
- 能使用文档命令启动；
- 核心页面可访问；
- 至少一个真实用户流程可完成；
- 错误能够被捕获并显示合理信息；
- 测试能够验证关键行为；
- 不依赖未记录的本地手工配置。

不要在最小闭环阶段同时实现全部功能。

---

## 12. 阶段 6：验证与交付

完成后执行：

1. 从干净环境重新安装；
2. 运行格式化、Lint 和类型检查；
3. 运行全部测试；
4. 执行生产构建；
5. 启动并完成最小用户流程；
6. 检查浏览器控制台和服务日志；
7. 检查 `git diff` 和意外生成文件；
8. 检查文档、代码和命令是否一致；
9. 检查是否残留密钥、调试数据和临时绕过；
10. 记录无法验证的项目及原因。

输出《基础设施交付报告》：

```md
## 已完成

- 文档：
- 工程配置：
- 测试与 CI：
- 最小运行闭环：

## 验证结果

- 安装：
- Lint：
- 类型检查：
- 测试：
- 构建：
- 冒烟测试：

## 用户仍需处理

- 外部账号或密钥：
- 部署配置：
- 未确认决策：

## 风险与后续建议

- 风险：
- 建议：
```

---

## 13. 阶段 7：会话交接和后续开发

### 13.1 `PROJECT_STATE.md` 最低内容

```md
# Project State

- 最后更新时间：
- 最后稳定提交：
- 当前里程碑：
- 当前活动规格或分支：
- 已完成功能：
- 正在进行：
- 已知问题：
- 阻塞决策：
- 最近验证结果：
- 下一步：
```

### 13.2 新会话恢复协议

新会话开始时：

1. 阅读 `AGENTS.md`；
2. 阅读 `PROJECT_STATE.md`；
3. 阅读当前活动规格或任务；
4. 检查 `git status` 和最近提交；
5. 不修改代码，先输出项目对齐摘要；
6. 用户确认后继续工作。

### 13.3 结束会话协议

结束前：

1. 检查 diff；
2. 运行与本次改动相关的验证；
3. 更新任务状态；
4. 更新 `PROJECT_STATE.md`；
5. 记录遗留问题和下一步；
6. 不掩盖失败测试或未完成内容。

---

# 智能体项目专项补充

以下内容只在项目包含 LLM、智能体、RAG、模型路由、工具调用或自主执行时追加。不得因为项目名称中出现“AI”就默认全部需要。

## 14. 智能体项目启动前必须额外确认

- 智能体究竟完成什么任务；
- 为什么该任务需要智能体，而不是普通确定性程序；
- 单智能体是否足够；
- 模型允许调用哪些工具；
- 哪些操作必须由用户确认；
- 输入、输出和结构化 Schema；
- 会话状态和长期记忆需求；
- 是否需要 RAG；
- 知识来源、更新频率和引用要求；
- 模型和 API 是否需要替换；
- 成本、延迟、并发和上下文预算；
- Prompt Injection、数据泄漏和越权风险；
- 如何评测正确率、工具调用和任务完成率；
- 模型或工具失败后的降级方案。

默认从单智能体、少量工具和最小状态开始。只有角色需要不同工具、权限、上下文或安全策略时，才考虑多智能体。

---

## 15. 智能体项目建议补充文件

| 文件 | 作用 | 创建条件 |
|---|---|---|
| `docs/AGENT_CONTRACTS.md` | 每个智能体的职责、输入、输出、工具、权限、停止条件和失败处理 | 所有智能体项目 |
| `docs/MODEL_PROVIDER_MATRIX.md` | 不同 API/模型的能力、限制、成本、延迟和验证状态 | 使用或可能切换多个模型/API |
| `docs/PROMPT_MANAGEMENT.md` | Prompt 结构、版本、变量、修改流程和回滚方式 | Prompt 影响核心行为 |
| `docs/TOOL_REGISTRY.md` | 工具名称、参数、权限、副作用、超时和幂等性 | 智能体调用工具 |
| `docs/EVAL_PLAN.md` | 数据集、指标、评分方法、基线和通过阈值 | 所有智能体项目 |
| `docs/EVAL_DATASET_GUIDE.md` | 样例来源、标注、版本、隐私和防止测试污染 | 维护长期评测集 |
| `docs/MEMORY_STRATEGY.md` | 短期状态、长期记忆、写入、检索、删除和隐私 | 存在记忆功能 |
| `docs/RAG_SPEC.md` | 文档来源、切分、索引、检索、引用、更新和评测 | 使用 RAG |
| `docs/GUARDRAILS.md` | 输入输出校验、注入防护、越权防护和人工审批 | 能访问外部数据或执行操作 |
| `docs/COST_LATENCY_BUDGET.md` | Token、费用、P50/P95 延迟、并发和降级阈值 | 需要控制成本或性能 |
| `docs/AGENT_OBSERVABILITY.md` | Trace、模型调用、工具调用、版本和错误记录 | 长期调优或生产运行 |
| `docs/DATA_PROVENANCE.md` | 数据来源、授权、更新时间、引用和删除机制 | 使用外部或用户数据 |
| `docs/THREAT_MODEL.md` | 资产、攻击面、信任边界、威胁和缓解措施 | 有高权限工具或敏感数据 |

---

## 16. 多模型与 API 切换基础设施

如果项目可能更换模型或 API，必须使用 Provider 抽象层，业务逻辑不得直接依赖某一家供应商。

统一接口至少覆盖：

- 文本或多模态输入；
- 普通生成与流式生成；
- 结构化输出；
- 工具调用；
- 超时、重试和取消；
- 使用量、成本和延迟记录；
- 模型能力检测；
- 错误归一化。

推荐目录：

```text
src/ai/
├── core/
│   ├── types
│   ├── schemas
│   └── errors
├── providers/
│   ├── provider-a
│   ├── provider-b
│   └── mock
├── routing/
├── prompts/
└── observability/
```

必须根据能力选择实现，而不是在业务代码中到处判断供应商名称。

每次评测至少记录：

```json
{
  "git_commit": "",
  "provider": "",
  "model": "",
  "prompt_version": "",
  "tool_schema_version": "",
  "dataset_version": "",
  "latency_ms": 0,
  "token_usage": {},
  "score": {},
  "errors": []
}
```

更换 API、模型、Prompt、工具 Schema 或检索策略后，必须运行同一组回归评测，不能仅凭主观体验宣布效果提升。

---

## 17. 智能体评测要求

普通软件测试和智能体评测必须同时存在。

### 17.1 确定性测试

- 数据校验；
- 工具参数；
- 权限；
- 数据库读写；
- API 契约；
- 超时与重试；
- 错误处理；
- 结构化输出 Schema。

### 17.2 智能体评测

- 任务完成率；
- 答案正确率；
- 引用和事实准确性；
- 幻觉率；
- 工具选择正确率；
- 工具参数正确率；
- 多轮状态保持；
- 安全和越权行为；
- 成本和延迟；
- 用户或评委 Rubric。

### 17.3 数据集组成

- 常规案例；
- 边界案例；
- 历史失败案例；
- 恶意或注入案例；
- 外部服务失败案例；
- 不应该触发智能体或工具的负样例。

每发现一次真实失败，应在修复后加入回归数据集。

---

## 18. 智能体项目完成定义

除通用项目完成条件外，还必须满足：

- 智能体职责和边界已记录；
- 工具权限符合最小权限原则；
- Prompt 和工具 Schema 有版本；
- 关键输出经过 Schema 或规则验证；
- 模型和工具失败有降级行为；
- Evals 达到已确认阈值；
- 评测结果可复现和比较；
- 调用链、模型、Prompt、工具和错误可追踪；
- API 切换不会要求重写核心业务；
- 敏感操作具备用户确认或人工审批；
- 成本和延迟未超过预算；
- 文档、规格、测试和实现保持一致。

---

## 19. Codex 最终输出要求

完成整个启动流程后，Codex 必须向用户报告：

1. 已确认的项目方案；
2. 创建或修改的基础设施；
3. 创建的文档及其作用；
4. 项目的安装、启动、测试和构建命令；
5. 实际运行过的验证及结果；
6. 尚未验证或需要用户提供的内容；
7. 当前项目状态和下一步；
8. 如果属于智能体项目，建议追加哪些专项文件以及原因。

不得用“应该可以”“大概没问题”代替真实验证。未运行的检查必须明确标记为“未验证”。

---

## 20. 启动指令

用户可以将下面内容发送给 Codex：

```text
请完整阅读仓库根目录的《Codex通用网页与智能体项目启动构建指南.md》并严格执行。

当前先执行阶段 0，只读检查项目，不要修改任何文件。完成项目现状报告后，按照指南分批询问我关键问题。不得自行假设产品需求、技术栈、数据来源、API、部署方式或智能体方案。只有在我明确确认完整方案后，才能创建文档和项目基础设施。
```

