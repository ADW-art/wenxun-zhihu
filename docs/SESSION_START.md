# 新会话恢复协议

最后更新：2026-09-24

新会话开始时按顺序执行：

1. 阅读 `AGENTS.md`。
2. 阅读 `docs/PROJECT_STATE.md`。
3. 阅读当前活动规格或 OpenSpec change。
4. 阅读相关需求和架构文档。
5. 执行 `git status`、`git log -5 --oneline` 和最近测试结果检查。
6. 先输出项目对齐摘要，不直接修改代码。
7. 用户确认后继续工作。
