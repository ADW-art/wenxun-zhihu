## 1. 代码视觉基础

- [x] 1.1 更新 `src/app/tokens.css` 为深墨绿、宣纸、朱红和土金语义 Token，并保留旧别名，运行 `npm run build` 验证
- [x] 1.2 重构 App Shell 和共享页面标题、指标、证据、状态组件，运行 `npm run typecheck` 和 UI 契约检查
- [x] 1.3 将示意地图与侧栏暗纹资产加入 `public/assets`，验证 Next.js 资源路径和构建

## 2. 核心页面

- [x] 2.1 新增 `/risk-map` 示意地图工作台，验证无精确坐标、响应式无溢出
- [x] 2.2 重构 `/dashboard` 巡查总览，验证指标、最近巡查、空状态和移动端布局
- [x] 2.3 重构 `/inspections/[id]` 证据与复核工作台，验证 AI 降级、待确认、规范来源和权限状态
- [x] 2.4 重构 `/tasks/[id]` 整改证据复核，验证提交、驳回、证据不足和只读状态
- [x] 2.5 重构 `/reports` 归档报告中心，验证可归档列表、下载入口和空状态

## 3. 验证与交付

- [x] 3.1 为新增共享组件和状态编写必要单元测试，运行 `npm test`
- [x] 3.2 运行格式、Lint、类型检查、UI 契约、集成测试和生产构建
- [x] 3.3 用 Playwright 检查 1440×1024 和 375×812，记录截图、横向溢出和控制台错误
- [x] 3.4 更新项目状态与交接文档，审查 diff 和密钥，创建 GitHub 里程碑提交并推送
